<div class="row">
  {{#each model as |question|}}
    {{question-list-col question=question}}
  {{else}}
    <div class="col-md-12">
	  <div class="panel question">
		<div class="panel-heading"><h2>Answer</h2></div>
		<div class="panel-body">There are no question yet on the site.</div>
		<div class="panel-footer">By Admin</div>
	  </div>
	</div>
  {{/each}}
</div>