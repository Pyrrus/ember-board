
import { voteColor } from 'blog/helpers/vote-color';
import { module, test } from 'qunit';

module('Unit | Helper | vote color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = voteColor([42]);
  assert.ok(result);
});

